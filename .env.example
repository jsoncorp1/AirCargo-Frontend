# Copiar a `.env.local` y completar. `.env.local` está en .gitignore: los valores
# reales nunca se versionan.

# ─── API ─────────────────────────────────────────────────────────────────────
# Incluye el prefijo de versión. Si no se define, el front cae en el localhost
# de desarrollo que ves acá abajo.
NEXT_PUBLIC_API_URL=https://localhost:7099/api/v1/core

