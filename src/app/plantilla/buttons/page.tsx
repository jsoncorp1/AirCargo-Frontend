import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Buttons | Plantilla AirCargo",
  description: "Pantalla de referencia de la plantilla TailAdmin.",
};

export { default } from "@/app/(admin)/(ui-elements)/buttons/page";
